# ── Core LLM & Agent ──────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
langchain-ollama>=0.1.0
langgraph>=0.1.0

# ── Vector store (semantic / Tier-3 memory) ───────────────────────────────────
chromadb>=0.5.0

# ── HTTP & downloads ──────────────────────────────────────────────────────────
requests>=2.31.0
tqdm>=4.66.0

# ── Voice I/O ─────────────────────────────────────────────────────────────────
pyttsx3~=2.90
SpeechRecognition>=3.10.0
PyAudio~=0.2.11

# ── Utilities ─────────────────────────────────────────────────────────────────
wikipedia~=1.4.0
python-decouple~=3.8
pygments~=2.17.2

# ── Vision (screen capture) ───────────────────────────────────────────────────
mss>=9.0.0
Pillow>=10.2.0

# ── Web UI (--web flag on desktop) ────────────────────────────────────────────
fastapi>=0.110.0
uvicorn>=0.29.0

# ── Desktop UI (PyQt6 overlay — Phase 2) ──────────────────────────────────────
# PyQt6>=6.6.0          # uncomment when building the animated overlay

# ── Mobile (llama.cpp backend — for Android / iOS builds) ─────────────────────
# llama-cpp-python>=0.2.0   # uncomment for mobile builds
